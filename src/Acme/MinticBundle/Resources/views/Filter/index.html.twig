{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Filter list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Count</th>
                <th>Money</th>
                <th>Percentage</th>
                <th>Points</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('filter_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.count }}</td>
                <td>{{ entity.money }}</td>
                <td>{{ entity.percentage }}</td>
                <td>{{ entity.points }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('filter_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('filter_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('filter_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
